/**
 * Configuración del entorno de producción.
 * Este archivo se utiliza para las compilaciones de producción y contiene la configuración específica del entorno de producción.
 */
export const environment = {
  production: true
};
