/**
 * Evita que la detección de cambios de Angular
 * se ejecute con ciertas devoluciones de llamada de componentes web
 */
// eslint-disable-next-line no-underscore-dangle
(window as any).__Zone_disable_customElements = true;
